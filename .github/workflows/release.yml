
name: Release Bot

on:
  pull_request:
    types: [closed]

jobs:
  build:
    name: Run CI Tasks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test
        run: ls -la
